# Match CI/Docker with local installs when peer deps (e.g. Sentry → React) are optional for a Nest API.
# Without this, `npm ci` in Docker fails while local `npm install` works if ~/.npmrc sets legacy-peer-deps.
legacy-peer-deps=true
